server:
    port: 8083

eureka:
    instance:
        prefer-ip-address: true
    client:
        service-url:
            defaultZone: http://35.238.241.13:8761/eureka/
        register-with-eureka: true

spring:
    application:
        name: auth

    jpa:
        show-sql: true
        hibernate:
            ddl-auto: create-drop
    jmx:
        enabled: false

    datasource:
        url: jdbc:sqlserver://projeto-bora.database.windows.net:1433;database=BoraDB;user=bora-project@projeto-bora;password=Projeto@bor;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
        username: bora-project
        password: Projeto@bor
        driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
        platform: mssql

jwt:
    config:
        type: signed
